
# 🏭 স্মার্ট ফ্যাক্টরি লিস্ট প্রো (Smart Factory List Pro)

এটি একটি আধুনিক এবং রিয়েল-টাইম ওয়েব অ্যাপ্লিকেশন যা ফ্যাক্টরি বা ওয়্যারহাউজের মালামাল (Export/Import) ট্র্যাকিং এবং লিস্টিং করার জন্য তৈরি করা হয়েছে। এটি খাতা-কলমের ঝামেলা কমিয়ে ডিজিটাল এবং স্মার্ট উপায়ে ডাটা ম্যানেজ করতে সাহায্য করে।

![Project Status](https://img.shields.io/badge/Status-Active-green)
![Technology](https://img.shields.io/badge/Built%20With-HTML%20%7C%20Tailwind%20%7C%20Supabase-blue)

## ✨ মূল ফিচারসমূহ (Key Features)

- **📷 স্মার্ট OCR স্ক্যানার:** ক্যামেরা ব্যবহার করে পণ্যের কোড স্ক্যান করার সুবিধা। এটি হাতের লেখা এবং প্রিন্টেড টেক্সট উভয়ই পড়তে পারে।
- **⚡ রিয়েল-টাইম ডাটাবেস:** Supabase ব্যবহার করায় এক ডিভাইসে ডাটা এন্ট্রি করলে অন্য সব ডিভাইসে সাথে সাথে আপডেট হয়ে যায়।
- **📄 অটোমেটিক PDF রিপোর্ট:** দিন বা শিফট শেষে এক ক্লিকেই সম্পূর্ণ লিস্টিং-এর PDF রিপোর্ট জেনারেট করা যায় (ম্যানেজারের স্বাক্ষরের জায়গাসহ)।
- **🔢 স্মার্ট ফরম্যাটিং:**
  - `1/2` বা `3/4` এর মতো ইনপুট দিলে তা গাণিতিক ভগ্নাংশ ($\frac{1}{2}$) আকারে সুন্দরভাবে শো করে।
  - `10pic` বা `10PCS` লিখলে তা অটোমেটিক `10Pcs` বা `10Pic` ফরম্যাটে ঠিক করে নেয়।
- **🔍 পাওয়ারফুল সার্চ:** গ্লোবাল সার্চ এবং নির্দিষ্ট তারিখের সার্চ সুবিধা, সাথে সার্চ রেজাল্ট হাইলাইটিং।
- **⚠️ ডুপ্লিকেট চেকার:** একই দিনে একই কোড ডুপ্লিকেট এন্ট্রি হলে সতর্ক করে এবং পুরাতন তারিখের এন্ট্রি হলে নোটিফিকেশন দেয়।
- **📱 রেসপন্সিভ ডিজাইন:** মোবাইল, ট্যাবলেট এবং ডেস্কটপ—সব ডিভাইসে পারফেক্টভাবে কাজ করে।

## 🛠 ব্যবহৃত টেকনোলজি (Tech Stack)

*   **Frontend:** HTML5, JavaScript (Vanilla), Tailwind CSS (CDN)
*   **Database:** Supabase (PostgreSQL)
*   **Icons:** Bootstrap Icons
*   **Libraries:**
    *   `Tesseract.js` (ইমেজ টু টেক্সট / OCR এর জন্য)
    *   `jsPDF` & `jspdf-autotable` (PDF রিপোর্ট তৈরির জন্য)

## 🚀 সেটআপ গাইড (Installation)

আপনার লোকাল কম্পিউটারে এটি রান করার জন্য নিচের ধাপগুলো অনুসরণ করুন:

১. রিপোজিটরি ক্লোন করুন:
```bash
git clone https://github.com/your-username/smart-factory-list.git
```

২. ফোল্ডারে প্রবেশ করুন:
```bash
cd smart-factory-list
```

৩. `index.html` ফাইলটি যেকোনো ব্রাউজারে ওপেন করুন।

> **নোট:** সম্পূর্ণ ফাংশনালিটি পাওয়ার জন্য আপনাকে Supabase সেটআপ করতে হবে।

## 🗄️ ডাটাবেস সেটআপ (Supabase SQL)

Supabase ড্যাশবোর্ডে গিয়ে **SQL Editor**-এ নিচের কোডটি রান করুন টেবিল তৈরি করার জন্য:

```sql
-- টেবিল তৈরি (আগেরটি ডিলিট করে নতুন করে)
DROP TABLE IF EXISTS public.daily_lists;

CREATE TABLE public.daily_lists (
    id bigint generated by default as identity primary key,
    date text not null,
    shift text not null,
    numbers jsonb default '[]'::jsonb,
    created_at timestamp with time zone default timezone('utc'::text, now()) not null,
    CONSTRAINT daily_lists_date_shift_key UNIQUE (date, shift)
);

-- সিকিউরিটি পলিসি (RLS)
ALTER TABLE public.daily_lists ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Enable all access for everyone" 
ON public.daily_lists FOR ALL USING (true) WITH CHECK (true);

-- রিয়েল-টাইম সাবস্ক্রিপশন
ALTER PUBLICATION supabase_realtime ADD TABLE public.daily_lists;
```

## ⚙️ কনফিগারেশন

`index.html` ফাইলের `<script>` সেকশনে আপনার নিজের Supabase প্রজেক্টের `URL` এবং `ANON KEY` বসিয়ে দিন:

```javascript
const SUPABASE_URL = "আপনার_সুপাবেস_লিঙ্ক";
const SUPABASE_KEY = "আপনার_সুপাবেস_কী";
```

## 📦 ডিপ্লয়মেন্ট (Deployment)

এই প্রজেক্টটি **Cloudflare Pages**, **Vercel**, বা **Netlify** তে খুব সহজেই হোস্ট করা যায়। এটি একটি স্ট্যাটিক সাইট, তাই কোনো বিল্ড কমান্ডের প্রয়োজন নেই।

**Cloudflare Pages Settings:**
- **Build command:** (খালি রাখুন)
- **Output directory:** `.` (অথবা `/`)

## 🤝 কন্ট্রিবিউশন

ফিচার রিকোয়েস্ট বা বাগ ফিক্সের জন্য নির্দ্বিধায় **Pull Request** করতে পারেন।

## 📝 লাইসেন্স

এই প্রজেক্টটি ওপেন সোর্স এবং MIT লাইসেন্সের আওতাভুক্ত।
```
